import test from'node:test';import assert from'node:assert/strict';import{anonymousMetric,applicationPerformance,performanceSummary}from'../src/performance.mjs';
test('performance telemetry contains durations and coarse dimensions without task or resume values',()=>{const task={taskId:'private-task',createdAt:'2026-01-01T00:00:00.000Z',factSnapshot:{facts:Array(12).fill({value:'private'})}},application={outcome:'partial',completedAt:'2026-01-01T00:00:08.000Z',performance:{openStartedAt:'2026-01-01T00:00:01.000Z',surfaceOpenedAt:'2026-01-01T00:00:02.000Z',formReadyAt:'2026-01-01T00:00:03.000Z',firstActionAt:'2026-01-01T00:00:04.000Z',executionPath:'recipe'},recipe:{recipeId:'a'.repeat(64)}};assert.equal(applicationPerformance(task,application).taskToCompleteMs,8000);const metric=anonymousMetric(task,application);assert.equal(metric.factCountBucket,'11-30');assert.equal(JSON.stringify(metric).includes('private-task'),false);assert.equal(JSON.stringify(metric).includes('private'),false);const summary=performanceSummary({one:{...task,applications:[application]}});assert.equal(summary.completedApplications,1);assert.equal(summary.timings.taskToCompleteMs.p95,8000);assert.equal(summary.knownPageReleaseGate.pass,null);assert.equal(summary.knownPageReleaseGate.p95TargetMs,20000)});
