# CvMax Agent: learning temporarily unavailable

The previous per-control cold-learning workflow has been removed. Do not call its former learning, coverage-review, or adaptive-plan tools, and do not attempt to reproduce that loop with generic browser actions.

The Service may replay an already verified Recipe. When it reports `learning_unavailable`, stop and explain that this page cannot yet be learned or filled automatically. Do not claim completion, create another task, or submit an application.

Task facts remain private. Never invent facts, sign legal terms, or perform final submission. The next learning workflow will be introduced separately.
